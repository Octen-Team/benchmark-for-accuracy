# Optional dependencies for the fetch evaluation lane. The core requirements.txt still
# holds nothing but requests.
#
# When any of these is missing, the corresponding feature is recorded as unavailable and
# reported as such. A missing dependency never crashes a run, and never counts as a
# provider failure.

# Local extraction libraries (these double as two of the providers under test)
trafilatura>=1.8
readability-lxml>=0.8
html2text>=2024.2.26

# Ground-truth parse channel: non-HTML documents
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6
feedparser>=6.0

# Ground-truth browser channel (also run: python -m playwright install chromium)
playwright>=1.40
